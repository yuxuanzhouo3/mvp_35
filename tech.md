# PickGlobal（MVP 35）技术栈

产品方案见 [project.md](project.md)。  
域名：`pickgrobal.mornscience.top`  
主路径：**腾讯云开发 CloudBase（国内）**，不用 Vercel / Supabase / 境外模型。

对照代码：`mvp_1` / `mvp_24` / `mvp_28` 国内版。

---

# Part A · 国内腾讯云技术（本项目采用）

## A1. 总览

| 能力 | 腾讯产品 | 用途 | 兄弟项目 |
| --- | --- | --- | --- |
| 代码运行 | CloudBase **云托管** | Next.js 15 SSR `standalone` | mvp_1 / 24 / 28 |
| 业务数据库 | CloudBase **云文档**（文档型） | 商品、报告、线索、触达、召回 | mvp_1 / 24 / 28；**不用 MySQL 作 DEMO 主库** |
| 对象存储 | CloudBase **云存储** / **COS** | 报告附件、数智人视频 | 同上 |
| 异步任务 | **云函数** / 定时触发 | 分析、渲染、发信、召回 | 同上 |
| AI 文案 / 分析 | **腾讯混元** API | 报告摘要、邮件文案、召回话术 | 国内主模型（不用 OpenAI） |
| 数字人视频 | **腾讯云智能数智人** | DEMO：**placeholder**（不买 10h 包） | — |
| 邮件 | **腾讯云 SES** | `SendEmail` + 回调 | — |
| 地图 / 线索（国内） | **腾讯位置服务** | 国内门店 / 地址类线索 | — |
| 客户端 | **微信小程序** + cnwww 壳 | 复用同一后端 | mvp_9 / 28 |
| SDK | `@cloudbase/js-sdk` / `@cloudbase/node-sdk` | 鉴权、DB、存储 | mvp_28 `CloudBaseConnector` |

```
Web / 小程序 / 原生壳
        ↓
CloudBase 云托管（Next.js standalone）
        ↓
文档型 DB · 云存储/COS · 云函数
        ↓
混元 · 数智人 · SES · 位置服务 · 自定义国内 API
```

应用层：`Next.js 15 + TypeScript + Tailwind + shadcn/ui`（兄弟共识）。

---

## A2. MySQL vs 云文档（文档型）——怎么选

CloudBase 建环境时选定主库类型（文档型 / MySQL / PostgreSQL 环境），**同一环境不能两套主库混用**。

### 对照表

| | **云文档（文档型）** | **MySQL** |
| --- | --- | --- |
| 形态 | 集合 + 文档（类似 Mongo） | 表 + 行（关系型） |
| SDK | `db.collection('products')` | 连接串 / 连接器 + SQL |
| 事务 / JOIN | 弱；多靠应用层拼 | 强；适合对账、复杂报表 |
| JSON 报告 / 状态机 | **很合适**（整份报告当文档） | 可用 JSON 列，但更啰嗦 |
| 微信小程序同栈 | **原生同环境** | 要另接 |
| 兄弟国内版 | mvp_28 / 24 / 1 **全用这个** | mvp_24 另有 mysql2 场景，非国内主路径 |
| DEMO 成本 | 随调用计资源点，一般更便宜起步 | CU 常驻更贵，闲时暂停可省 |
| 运维 | 零表结构迁移心智 | 要 migrations |

### 本项目结论

| 决策 | |
| --- | --- |
| **DEMO / Phase A** | **云文档 `[✓]`** |
| MySQL | **暂不上**；等出现强对账、多表 JOIN、BI 再评估 |
| PostgreSQL | **不用** |

理由（一句话）：闭环数据是「文档 + 状态」（商品报告、触达记录、召回任务），云文档够用，且能直接抄 mvp_28 connector；MySQL 不是不能用，是 **DEMO 阶段没必要多付一层关系库成本与迁移成本**。

### 集合（云文档）

| 集合 | 用途 |
| --- | --- |
| `products` | 选品 / 自有商品 |
| `product_sources` | csv / custom / 国内电商 API |
| `analysis_reports` | 利润 / 税务 / 时间 / 风险 |
| `leads` | 线索 |
| `campaigns` | 活动 |
| `outreach_messages` | 邮件草稿与发送 |
| `digital_human_assets` | 数智人任务（DEMO 存 placeholder 状态） |
| `recall_jobs` | 流失召回 |
| `provider_credentials` | 密钥引用（加密字段） |
| `audit_logs` | 审计 |

---

## A3. 云托管（Code）

| 项 | 约定 |
| --- | --- |
| 形态 | Docker → CloudBase 云托管 |
| 应用 | Next.js App Router，`output: 'standalone'` |
| 环境变量 | 只挂云托管，不进前端包 |
| 对照 | mvp_28 / 24 / 1 |

| 变量 | 说明 |
| --- | --- |
| `WECHAT_CLOUDBASE_ID` / `NEXT_PUBLIC_WECHAT_CLOUDBASE_ID` | 环境 ID |
| `CLOUDBASE_SECRET_ID` / `CLOUDBASE_SECRET_KEY` | 服务端密钥 |
| `HUNYUAN_*` | 混元 API |
| `SES_*` / 数智人密钥 | 触达 |
| 域名 | `pickgrobal.mornscience.top` |

```
cloudbase/
├── Dockerfile
└── (集合初始化脚本 / 权限规则)
```

---

## A4. 云存储 / COS

| 存什么 | 说明 |
| --- | --- |
| 分析报告附件 | PDF / 导出包 |
| 数智人成片 | 渲染后写入，邮件附链 |
| 临时素材 | 旁白、形象素材 |

---

## A5. 云函数 / Jobs

| Job | 角色 |
| --- | --- |
| 商品导入 / 分析 | 写 `products` / `analysis_reports` |
| 混元生成 | 报告摘要、邮件 / 召回文案 |
| 数智人渲染 | API → COS → `digital_human_assets` |
| SES 发送 | `outreach_messages` → `SendEmail` |
| 流失召回 | `recall_jobs` → 再入发送队列 |
| SES 回调 | 打开 / 退信 / 投诉回流 |

---

## A6. AI：仅国内模型

| 用途 | 模型 | 备注 |
| --- | --- | --- |
| 文案 / 分析主模型 | **腾讯混元**（如 Hunyuan-Turbos / 业务可用档） | 不用 OpenAI / Claude / Gemini |
| 可选补充 | 通义千问（阿里云，仍属国内） | 仅当混元额度不足时；默认不引入 |

---

## A7. 数智人 + SES

| 项 | DEMO / 现阶段 | 以后正式 |
| --- | --- | --- |
| 数智人 | **不买 10h 视频包**；UI / 链路用 **placeholder**（假封面、假视频 URL、状态 mock：`ready`） | 再购形象 + 小时包，接真实 API |
| SES | 可用真实发信或同样 mock（视域名是否就绪） | 正式域名 + 回调 |
| 原则 | 不走境外邮件 SaaS；视频成本先压到 **¥0** | — |

---

## A8. 获客 / 商品数据（仅国内通道）

| 能力 | 采用 | 不用 |
| --- | --- | --- |
| 商品 | 手动 / CSV + **自定义国内 API**（1688 等自有对接） | Amazon Creators API |
| 线索 | **腾讯位置服务** + 自定义 CRM / 企查查类国内 API | Google Places |
| 税务市场 | 中国 + 美国规则引擎（算税逻辑本地化；数据源仍国内托管） | — |

---

## A9. 成本估算（各部分 · 人民币）

**DEMO 锁定：不买数智人 10h 视频包**；视频位用 placeholder，该项 **¥0**。  
假设：小流量（约几十卖家、分析 + 邮件为主）。价格以控制台为准。

| 部分 | 计费方式（概要） | DEMO 月估 | 备注 |
| --- | --- | --- | --- |
| CloudBase 套餐 | 个人 ¥19.9 / 标准 ¥199 | **¥20–199** | DEMO 可先个人版 |
| 云托管 | ~¥0.055/核·时 + ~¥0.032/GB·时 | **¥80–200** | 最小常驻 |
| 云文档 DB | 调用 + 容量（资源点） | **¥20–60** | 主库 |
| 云存储 / CDN | 容量 + 流量 | **¥10–40** | 无真实视频流量 |
| 云函数 | 调用 + GBs | **¥10–40** | |
| 混元 | tokens | **¥50–150** | 报告 + 邮件文案 |
| SES | ¥0.0019/封（含 1k 免费） | **¥0–20** | |
| **数智人** | — | **¥0** | **placeholder only；不买 10h 包** |
| 位置服务 | 按次 | **¥0–50** | |
| **DEMO 合计** | | **约 ¥200–800/月** | 视套餐档位 |

以后若上正式数智人：再单独加形象 / 小时包（例如 10h 包约 ¥2,000 档），**不计入当前 DEMO 预算**。

分档扩容方案见 **Part C**（1万 / 10万 / 100万用户）。

---

## A10. 密钥清单（仅国内）

- CloudBase 环境 ID、`CLOUDBASE_SECRET_ID` / `CLOUDBASE_SECRET_KEY`
- 混元 API Key
- SES 发信域名与密钥、回调 URL
- 数智人 AppKey / AccessToken / 形象 ID
- 腾讯位置服务 Key（若用）
- 自定义商品 / 线索 API 鉴权
- （Phase B）微信小程序 AppID / AppSecret

---

## A11. 明确不用

- Vercel 主发布  
- Supabase / Firebase / 境外 PG  
- PostgreSQL 作 CloudBase 主库（本项目）  
- OpenAI / Claude / Gemini 作主模型  
- Amazon / Google Places 作主数据源  
- 境外邮件 SaaS  

---

# Part C · 用户规模方案（1万 / 10万 / 100万）

「用户」= **注册卖家账号**（B 端）。  
粗算活跃：MAU ≈ 注册的 **15–25%**；每人月：分析 ~5 次、邮件触达 ~20 封、线索检索若干。  
数智人：**各档默认仍可不买视频包**（邮件为主）；若开视频，成本另列且会暴涨。

### C0. 一览

| 档 | 注册用户 | 架构主调 | DB | 月成本量级（无视频包） |
| --- | --- | --- | --- | --- |
| S1 | **1 万** | 单环境云开发拉满 | **云文档** | **约 ¥0.3万–1.2万** |
| S2 | **10 万** | 读写拆分 + 队列 + 可加 MySQL | 云文档为主，**热点上 MySQL** | **约 ¥1.5万–5万** |
| S3 | **100 万** | 多服务 / 多环境 / 专有云资源 | **MySQL（CDB）主库** + 云文档辅 | **约 ¥15万–50万+** |

---

### C1 · 1 万用户（S1）

**目标：** 生产可用，仍在云开发套餐内弹性。

| 层 | 方案 |
| --- | --- |
| 计算 | 云托管 **2–4 副本**（1 核 2GB 起），自动扩缩 |
| DB | **云文档**；关键集合建索引（`userId` / `status` / `createdAt`） |
| 任务 | 云函数 + 定时器；发送/分析异步化，避免 SSR 里同步打混元 |
| 存储 | 云存储；报告 PDF；视频仍 placeholder |
| AI | 混元；设每用户日限额 + 全局限流 |
| 邮件 | SES 按量；发信域名验证；可视情况 **1 个独立 IP（¥900/月）** |
| 套餐 | CloudBase **标准版 ¥199** 起，超限买资源包；偏稳用 **企业版 ¥999** |
| 观测 | 云开发监控 + 基础告警（错误率 / 延迟 / 余额） |

**不做：** 不上 MySQL、不上 TKE、不买数智人小时包。

**月费用粗拆（无视频）：**

| 项 | 约 |
| --- | --- |
| 套餐 + 云托管 + 云文档 + 存储 | ¥0.2万–0.5万 |
| 混元（假设 MAU 2k × 分析/文案） | ¥0.1万–0.5万 |
| SES（假设月 10万–30万封） | ¥200–600（+ 可选独立 IP ¥900） |
| **合计** | **≈ ¥0.3万–1.2万** |

---

### C2 · 10 万用户（S2）

**目标：** 扛并发与邮件峰值；开始「文档不够用的部分」迁关系库。

| 层 | 方案 |
| --- | --- |
| 计算 | 云托管多副本（按 CPU）；静态/API **分路径**；CDN 挡前端 |
| DB | **云文档保留**：报告正文、活动配置、日志类；**MySQL 上线**：用户/订阅/订单/配额/触达队列表（强一致） |
| 缓存 | 腾讯云 **Redis**（会话、限额、热点商品） |
| 队列 | **CMQ / TDMQ** 承接：分析、发信、召回（削峰） |
| 任务 | 云函数 worker 扩容；长任务勿堵在请求线程 |
| AI | 混元 **批量/异步**；失败重试；按套餐计费挡 |
| 邮件 | SES **资源包** + **独立 IP ≥1**；退信/投诉自动抑制 |
| 数智人 | 仍默认关；若开：仅 VIP，配额极严 |
| 套餐 | 企业版 / 企业高级 + 资源包；MySQL CU 常驻（或迁自有账号 VPC） |
| 多端 | 小程序 + Web 同 API；鉴权统一 |

**架构示意：**

```
客户端 → CDN → 云托管（Next）
                ├─ 云文档（报告 / 松散状态）
                ├─ MySQL（账户 / 配额 / 任务队列元数据）
                ├─ Redis
                └─ TDMQ → 云函数 workers → 混元 / SES
```

**月费用粗拆（无视频）：**

| 项 | 约 |
| --- | --- |
| 云托管 + CDN + 套餐 | ¥0.5万–1.5万 |
| 云文档 + MySQL + Redis | ¥0.4万–1.5万 |
| 队列 + 函数 | ¥0.1万–0.4万 |
| 混元 | ¥0.5万–2万 |
| SES（假设月 100万–500万封） | ¥0.2万–1万 + IP |
| **合计** | **≈ ¥1.5万–5万** |

**何时从 S1 升 S2：** 云文档单集合扫描变慢、对账/会员算不清、发信峰值压垮同步 API。

---

### C3 · 100 万用户（S3）

**目标：** 平台级；云开发仍可作边缘/小程序，**核心交易与任务上专有云资源**。

| 层 | 方案 |
| --- | --- |
| 计算 | **TKE / 云托管多服务**：`web` / `api` / `worker` 拆分；多可用区 |
| DB | **CDB MySQL 主从 + 只读**；分库分表按 `userId`；云文档仅配置/运营内容 |
| 缓存 / 搜 | Redis 集群；必要时 ES（线索检索） |
| 队列 | TDMQ 多 Topic（分析 / 触达 / 召回 / 回调） |
| 存储 | COS + CDN；大文件生命周期（冷存） |
| AI | 混元企业配额；本地规则引擎扛税务计算，LLM 只做叙述 |
| 邮件 | SES 大资源包 + **多独立 IP** + 子域名隔离营销/事务信 |
| 数智人 | 独立产品线/增值包；与主站配额隔离，防拖垮账单 |
| 安全 | WAF、审计、密钥轮转、等保视客户要求 |
| 环境 | `dev` / `test` / `pro` 隔离（对齐 workflow 分支） |

**架构示意：**

```
客户端 → CDN / WAF → API 网关
                      ├─ web / api（TKE 或云托管集群）
                      ├─ MySQL 主从（分片）
                      ├─ Redis / ES
                      ├─ TDMQ → worker 池
                      └─ COS · 混元 · SES ·（可选）数智人
小程序 → 仍可走云开发鉴权，业务 API 打到同一网关
```

**月费用粗拆（无全量视频）：**

| 项 | 约 |
| --- | --- |
| 计算 + 网关 + CDN + WAF | ¥3万–10万 |
| MySQL 集群 + Redis + ES | ¥3万–12万 |
| 队列 + 观测 + 备份 | ¥1万–3万 |
| 混元（重度需控） | ¥5万–20万 |
| SES（千万级封） | ¥2万–10万+ |
| **合计** | **≈ ¥15万–50万+** |

**若 1% 用户开数智人视频：** 可能再 dual 数万～数十万/月——必须做成增值付费，不能含在基础套餐。

---

### C4. 升级触发条件（简表）

| 信号 | 动作 |
| --- | --- |
| 注册破 ~1万、API P95 升高 | 加副本、索引、异步化（仍在 S1） |
| 对账/会员/配额算不动；发信要排队 | **进 S2**：MySQL + 队列 + Redis |
| 单库写满、多 AZ、大客户要 SLA | **进 S3**：CDB 分片 + 服务拆分 |
| 混元账单陡增 | 规则引擎替代部分 LLM；缓存报告 |
| 投诉率升 | SES IP/域名隔离；召回降频 |

### C5. 与 DEMO 的关系

| 现在 | 以后 |
| --- | --- |
| 按 **S1 缩小版** 建（云文档 + 单托管 + placeholder） | 代码预留：任务进队、`userId` 索引、配额字段 |
| 不买 10h 视频包 | S2/S3 再开数智人增值 |

---

# Part B · 兄弟 MVP 国内对照（精简）

| MVP | 国内相关 | 对本项目 |
| --- | --- | --- |
| 28 | CloudBase 文档库 + 云存储 + 微信登录 | **主对照** connector / auth |
| 24 | CloudBase 国内支付与用户 | 业务写入模式 |
| 1 | CN ↔ CloudBase / EN ↔ Supabase | 只抄 CN 半边 |
| 9 | `clients/` 多端 | Phase B |
| 其它 | 大量 Next + shadcn | UI 基线；忽略其 Vercel/Supabase |

---

# 总结

- **DB**：DEMO / 1万 → **云文档**；10万 → 文档 + **MySQL**；100万 → **MySQL 主库**。  
- **数智人**：各档默认可关；DEMO **placeholder**，不买 10h 包。  
- **成本量级（无视频）**：1万 ≈ **0.3–1.2万/月**；10万 ≈ **1.5–5万/月**；100万 ≈ **15–50万+/月**。